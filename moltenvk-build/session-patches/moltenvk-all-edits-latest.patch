diff --git a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
index c492057..b193884 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
@@ -215,6 +215,12 @@ void MVKCmdDraw::encodeIndexedIndirect(MVKCommandEncoder* cmdEncoder) {
 
 void MVKCmdDraw::encode(MVKCommandEncoder* cmdEncoder) {
 
+	// L4D2-launcher diagnostic: skip all draws to test whether the 0x010c is caused by
+	// draw/shader work vs render-pass structure. Gated by L4D2_MVK_NO_DRAWS=1.
+	static int s_l4d2NoDraws = -1;
+	if (s_l4d2NoDraws < 0) { const char* e = getenv("L4D2_MVK_NO_DRAWS"); s_l4d2NoDraws = (e && e[0] == '1') ? 1 : 0; }
+	if (s_l4d2NoDraws) { return; }
+
 	if (_vertexCount == 0 || _instanceCount == 0) { return; }	// Nothing to do.
 
 	cmdEncoder->restartMetalRenderPassIfNeeded();
@@ -426,6 +432,11 @@ void MVKCmdDrawIndexed::encodeIndexedIndirect(MVKCommandEncoder* cmdEncoder) {
 
 void MVKCmdDrawIndexed::encode(MVKCommandEncoder* cmdEncoder) {
 
+	// L4D2-launcher diagnostic: skip all draws (see MVKCmdDraw::encode). L4D2_MVK_NO_DRAWS=1.
+	static int s_l4d2NoDraws = -1;
+	if (s_l4d2NoDraws < 0) { const char* e = getenv("L4D2_MVK_NO_DRAWS"); s_l4d2NoDraws = (e && e[0] == '1') ? 1 : 0; }
+	if (s_l4d2NoDraws) { return; }
+
 	if (_indexCount == 0 || _instanceCount == 0) { return; }	// Nothing to do.
 
 	cmdEncoder->restartMetalRenderPassIfNeeded();
diff --git a/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.h b/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.h
index 2a6dcb8..247884b 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.h
+++ b/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.h
@@ -258,6 +258,12 @@ public:
     void encodeCommands(MVKCommand* command);
     void endEncoding();
 
+	// L4D2-launcher: command-buffer splitting state (see beginMetalRenderPass). _l4d2Submission is set
+	// by MVKCommandBuffer::submit() so the encoder can rotate the submission's active MTLCommandBuffer
+	// after every N top-level render passes, avoiding the per-command-buffer aggregate 0x010c fault.
+	MVKQueueCommandBufferSubmission* _l4d2Submission = nullptr;
+	uint32_t _l4d2PassCount = 0;
+
 	/** Encode commands from the specified secondary command buffer onto the Metal command buffer. */
 	void encodeSecondary(MVKCommandBuffer* secondaryCmdBuffer);
 
diff --git a/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.mm b/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.mm
index 707c267..95a0035 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCommandBuffer.mm
@@ -308,6 +308,7 @@ void MVKCommandBuffer::submit(MVKQueueCommandBufferSubmission* cmdBuffSubmit,
 		clearPrefilledMTLCommandBuffer();
 	} else {
 		MVKCommandEncoder encoder(this);
+		encoder._l4d2Submission = cmdBuffSubmit;	// L4D2-launcher: enable command-buffer splitting
 		encoder.encode(cmdBuffSubmit->getActiveMTLCommandBuffer(), pEncodingContext);
 	}
 
@@ -764,6 +765,27 @@ void MVKCommandEncoder::beginMetalRenderPass(MVKCommandUse cmdUse) {
 
     endCurrentMetalEncoding();
 
+	// L4D2-launcher: split the Metal command buffer after N top-level render passes so the heavy
+	// first-full-scene HDR frame spans several smaller command buffers instead of one oversized one.
+	// The 0x010c is triggered by the per-command-buffer aggregate (proven: it persists with all draws
+	// AND all MSAA resolves disabled). Rotate ONLY at a genuine render-pass begin — never a subpass or
+	// restart, since a Metal render pass cannot span command buffers. Queue command buffers execute
+	// serially in commit order, so cross-buffer ordering is preserved without extra barriers.
+	// Gated by L4D2_MVK_MAX_PASSES (0/unset = off).
+	{
+		static int s_l4d2MaxPasses = -1;
+		if (s_l4d2MaxPasses < 0) { const char* e = getenv("L4D2_MVK_MAX_PASSES"); s_l4d2MaxPasses = e ? atoi(e) : 0; }
+		bool isGenuineBegin = (cmdUse == kMVKCommandUseBeginRenderPass || cmdUse == kMVKCommandUseBeginRendering);
+		if (s_l4d2MaxPasses > 0 && _l4d2Submission && isGenuineBegin) {
+			if (_l4d2PassCount >= (uint32_t)s_l4d2MaxPasses) {
+				_mtlCmdBuffer = _l4d2Submission->l4d2RotateActiveMTLCommandBuffer();
+				_l4d2PassCount = 0;
+				if (getenv("MVK_L4D2_DEBUG")) { fprintf(stderr, "[mvk-l4d2-split] rotated MTLCommandBuffer after %d passes\n", s_l4d2MaxPasses); }
+			}
+			_l4d2PassCount++;
+		}
+	}
+
 	bool isRestart = cmdUse == kMVKCommandUseRestartSubpass;
     MTLRenderPassDescriptor* mtlRPDesc = [MTLRenderPassDescriptor renderPassDescriptor];
 	getSubpass()->populateMTLRenderPassDescriptor(mtlRPDesc,
@@ -827,6 +849,28 @@ void MVKCommandEncoder::beginMetalRenderPass(MVKCommandUse cmdUse) {
 		[mtlRPDesc setSamplePositions: sampPosns.data() count: sampPosns.size()];
 	}
 
+	// L4D2-launcher FIX — THE HDR 0x010c FIX (2026-06-04): skip attachment-less render passes.
+	// A Metal render command encoder with NO color/depth/stencil attachments hard-aborts this Apple
+	// GPU with Internal Error 0x010c (IOGPUCommandQueueErrorDomain 268), surfaced by MoltenVK as
+	// VK_ERROR_OUT_OF_DEVICE_MEMORY/DEVICE_LOST. Pinpointed via N=1-passes-per-command-buffer + synch-
+	// ronous submission as the EXACT faulting command buffer: an empty attachment-less encoder faults
+	// on its own, even with every draw AND every MSAA resolve skipped — so it is the pass's existence,
+	// not its contents. DXVK emits these (framebuffer 16384x16384, zero attachments) for HDR-only
+	// operations, which is why the freeze was HDR-specific and fired ~25-40s in at the first full-
+	// scene frame. Skipping the encode leaves _mtlRenderEncoder nil (already nil'd by
+	// endCurrentMetalEncoding above), so any draws recorded into the pass become safe no-ops; the next
+	// real render pass re-establishes all encoder state. User-verified: full campaign levels playable
+	// at max settings (4x MSAA + multicore + native res), HDR on, no freeze.
+	// DEFAULT ON; set L4D2_MVK_SKIP_NOATT=0 to disable (to reproduce/measure the original fault).
+	{
+		static int s_skipNoatt = -1;
+		if (s_skipNoatt < 0) { const char* e = getenv("L4D2_MVK_SKIP_NOATT"); s_skipNoatt = (e && e[0] == '0') ? 0 : 1; }
+		if (s_skipNoatt && !mtlRPDesc.colorAttachments[0].texture && !mtlRPDesc.depthAttachment.texture && !mtlRPDesc.stencilAttachment.texture) {
+			if (getenv("MVK_L4D2_DEBUG")) { fprintf(stderr, "[mvk-l4d2-skipnoatt] skipped attachment-less render pass\n"); }
+			return;
+		}
+	}
+
     _mtlRenderEncoder = [_mtlCmdBuffer renderCommandEncoderWithDescriptor: mtlRPDesc];
 	retainIfImmediatelyEncoding(_mtlRenderEncoder);
 	_cmdBuffer->setMetalObjectLabel(_mtlRenderEncoder, getMTLRenderCommandEncoderName(cmdUse));
diff --git a/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm b/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm
index 16fd614..2c51d31 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm
@@ -1893,6 +1893,14 @@ void MVKOcclusionQueryCommandEncoderState::endMetalRenderPass(MVKCommandEncoder*
 // In most cases, a MTLCommandBuffer corresponds to a Vulkan command submit (VkSubmitInfo),
 // and so the error text is framed in terms of the Vulkan submit.
 void MVKOcclusionQueryCommandEncoderState::beginOcclusionQuery(MVKCommandEncoder* cmdEncoder, MVKOcclusionQueryPool* pQueryPool, uint32_t query, VkQueryControlFlags flags) {
+	// L4D2-launcher diagnostic: optionally disable occlusion-query visibility tracking to test
+	// whether the HDR auto-exposure occlusion path triggers the 0x010c. Gated by L4D2_MVK_NO_OCCLUSION=1.
+	static int s_l4d2NoOcc = -1;
+	if (s_l4d2NoOcc < 0) {
+		const char* e = getenv("L4D2_MVK_NO_OCCLUSION"); s_l4d2NoOcc = (e && e[0] == '1') ? 1 : 0;
+		if (getenv("MVK_L4D2_DEBUG")) { fprintf(stderr, "[mvk-l4d2-occ] occlusion-query tracking %s\n", s_l4d2NoOcc ? "DISABLED" : "enabled"); }
+	}
+	if (s_l4d2NoOcc) { return; }
 	assert(!_currentPool && "Shouldn't have active query when beginning a new one!");
 	bool shouldCount = mvkAreAllFlagsEnabled(flags, VK_QUERY_CONTROL_PRECISE_BIT);
 	_currentVisibilityResultMode = shouldCount ? MTLVisibilityResultModeCounting : MTLVisibilityResultModeBoolean;
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm b/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm
index 00db8b0..8e5efa8 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm
@@ -880,7 +880,16 @@ template <MVKArgumentBufferMode Layout> struct MVKArgBufEncoder;
 /** Argument buffer encoder for Metal 3 encoding */
 template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::Metal3> {
 	MVKGPUResource* dst;
-	MVKArgBufEncoder(id<MTLArgumentEncoder> enc, char* base): dst(reinterpret_cast<MVKGPUResource*>(base)) {}
+	// L4D2-launcher patch: cached fallback GPU resource handles for null descriptor slots
+	uint64_t nullBufAddr;
+	MTLResourceID nullTexID;
+	MTLResourceID nullSampID;
+	MVKArgBufEncoder(id<MTLArgumentEncoder> enc, char* base,
+					  id<MTLBuffer> nullBuf, id<MTLTexture> nullTex, id<MTLSamplerState> nullSamp):
+		dst(reinterpret_cast<MVKGPUResource*>(base)),
+		nullBufAddr(nullBuf ? nullBuf.gpuAddress : 0),
+		nullTexID(nullTex ? nullTex.gpuResourceID : MTLResourceID{}),
+		nullSampID(nullSamp ? nullSamp.gpuResourceID : MTLResourceID{}) {}
 	void advance(size_t stride) { dst = reinterpret_cast<MVKGPUResource*>(reinterpret_cast<char*>(dst) + stride); }
 	void* constantData(size_t index) { return reinterpret_cast<char*>(dst) + index; }
 	void setTexture(id<MTLTexture> tex,       size_t index = 0) { dst[index].resource = tex.gpuResourceID; }
@@ -888,9 +897,10 @@ template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::Metal3> {
 	void setBuffer(id<MTLBuffer> buf, uint64_t offset, size_t index = 0) {
 		dst[index].gpuAddress = buf.gpuAddress + offset;
 	}
-	void setNullTexture(size_t index = 0) { dst[index].resource = {}; }
-	void setNullSampler(size_t index = 0) { dst[index].resource = {}; }
-	void setNullBuffer (size_t index = 0) { dst[index].gpuAddress = 0; }
+	// L4D2-launcher patch: bind dummies instead of zero so GPU dereferences don't fault
+	void setNullTexture(size_t index = 0) { dst[index].resource = nullTexID; }
+	void setNullSampler(size_t index = 0) { dst[index].resource = nullSampID; }
+	void setNullBuffer (size_t index = 0) { dst[index].gpuAddress = nullBufAddr; }
 	void setTexture(MVKImageView* img, size_t index = 0) { setTexture(img ? img->getMTLTexture() : nil, index); }
 	void setSampler(MVKSampler* samp, size_t index = 0) { setSampler(samp ? samp->getMTLSamplerState() : nil, index); }
 	void setBuffer(const VkDescriptorBufferInfo* info, size_t index = 0) {
@@ -905,7 +915,13 @@ template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::Metal3> {
 template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::ArgEncoder> {
 	id<MTLArgumentEncoder> enc;
 	size_t base;
-	MVKArgBufEncoder(id<MTLArgumentEncoder> enc_, char* base): enc(enc_), base(0) {}
+	// L4D2-launcher patch: fallback resources for null descriptor slots
+	id<MTLBuffer> nullBuf;
+	id<MTLTexture> nullTex;
+	id<MTLSamplerState> nullSamp;
+	MVKArgBufEncoder(id<MTLArgumentEncoder> enc_, char* base_unused,
+					  id<MTLBuffer> nullBuf_, id<MTLTexture> nullTex_, id<MTLSamplerState> nullSamp_):
+		enc(enc_), base(0), nullBuf(nullBuf_), nullTex(nullTex_), nullSamp(nullSamp_) {}
 	void advance(size_t stride) { base += stride; }
 	void* constantData(uint32_t index) { return [enc constantDataAtIndex:base + index]; }
 	void setTexture(id<MTLTexture> tex,       size_t index = 0) { [enc setTexture:tex atIndex:base + index]; }
@@ -913,9 +929,10 @@ template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::ArgEncoder> {
 	void setBuffer(id<MTLBuffer> buf, uint64_t offset, size_t index = 0) {
 		[enc setBuffer:buf offset:static_cast<NSUInteger>(offset) atIndex:base + index];
 	}
-	void setNullTexture(size_t index = 0) { [enc setTexture:nil atIndex:base + index]; }
-	void setNullSampler(size_t index = 0) { [enc setSamplerState:nil atIndex:base + index]; }
-	void setNullBuffer (size_t index = 0) { [enc setBuffer:nil offset:0 atIndex:base + index]; }
+	// L4D2-launcher patch: bind dummies instead of nil
+	void setNullTexture(size_t index = 0) { [enc setTexture:nullTex atIndex:base + index]; }
+	void setNullSampler(size_t index = 0) { [enc setSamplerState:nullSamp atIndex:base + index]; }
+	void setNullBuffer (size_t index = 0) { [enc setBuffer:nullBuf offset:0 atIndex:base + index]; }
 	void setTexture(MVKImageView* img, size_t index = 0) {
 		[enc setTexture:img ? img->getMTLTexture() : nil atIndex:base + index];
 	}
@@ -946,11 +963,22 @@ static void writeDescriptorSetGPUBuffer(
 	const MVKDescriptorBinding& binding,
 	id<MTLArgumentEncoder> enc_, char* base, const uint32_t* auxOffsets,
 	const void* src, size_t srcStride, MVKDescriptorUpdateSourceType srcType,
-	uint32_t start, uint32_t count)
+	uint32_t start, uint32_t count,
+	MVKDevice* device)	// L4D2-launcher patch: device for null-descriptor fallback buffer
 {
 	constexpr size_t dstStride = descriptorGPUStride(ArgBufMode, Layout);
 	size_t startOffset = start * dstStride;
-	MVKArgBufEncoder<ArgBufMode> enc(enc_, base);
+	// L4D2-launcher patch: gather fallback resources for null descriptor slots.
+	// We tried splitting read vs write dummies based on descriptorType, but
+	// VK_DESCRIPTOR_TYPE_STORAGE_BUFFER means "shader CAN write," not "will."
+	// DXVK uses STORAGE_BUFFER for read paths too, and routing those to an
+	// uninitialized sacrificial buffer made them read garbage → GPU faults.
+	// Single shared zero-filled dummies for everything; cross-shader write
+	// pollution is the documented trade-off (rare visual artifact).
+	id<MTLBuffer>       nullBuf  = device ? device->getDummyNullDescriptorMTLBuffer()  : nil;
+	id<MTLTexture>      nullTex  = device ? device->getDummyNullDescriptorMTLTexture() : nil;
+	id<MTLSamplerState> nullSamp = device ? device->getDummyNullDescriptorMTLSampler() : nil;
+	MVKArgBufEncoder<ArgBufMode> enc(enc_, base, nullBuf, nullTex, nullSamp);
 	uint32_t baseOffset = ArgBufMode == MVKArgumentBufferMode::ArgEncoder ? binding.argBufID : binding.gpuOffset;
 
 	if (Layout == MVKDescriptorGPULayout::OutlinedData || Layout == MVKDescriptorGPULayout::InlineData) {
@@ -1071,7 +1099,7 @@ static void writeDescriptorSetGPUBuffer(
 	const uint32_t* auxOffsets = set->auxIndices;
 	if (canUseFastPathUpdate(binding->gpuLayout, ArgBufMode)) {
 		switch (binding->gpuLayout) {
-#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(*binding, enc, base, auxOffsets, src, srcStride, srcType, start, count)
+#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(*binding, enc, base, auxOffsets, src, srcStride, srcType, start, count, set->layout->getDevice())	/* L4D2-launcher patch: pass device */
 			case MVKDescriptorGPULayout::None:          break;
 			case MVKDescriptorGPULayout::Texture:       DISPATCH(Texture);       break;
 			case MVKDescriptorGPULayout::Sampler:       DISPATCH(Sampler);       break;
@@ -1101,7 +1129,7 @@ static void writeDescriptorSetGPUBuffer(
 		while (true) {
 			uint32_t numWrite = std::min(count, binding->descriptorCount - start);
 			switch (binding->gpuLayout) {
-#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(*binding, enc, base, auxOffsets, src, srcStride, srcType, start, numWrite)
+#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(*binding, enc, base, auxOffsets, src, srcStride, srcType, start, numWrite, set->layout->getDevice())	/* L4D2-launcher patch: pass device */
 				case MVKDescriptorGPULayout::TexBufSoA:     DISPATCH(TexBufSoA);    break;
 				case MVKDescriptorGPULayout::TexSampSoA:    DISPATCH(TexSampSoA);   break;
 				case MVKDescriptorGPULayout::Tex2SampSoA:   DISPATCH(Tex2SampSoA);  break;
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h
index c9ce14a..754a408 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h
@@ -928,6 +928,34 @@ public:
 	 */
 	id<MTLBuffer> getDummyBlitMTLBuffer();
 
+	/**
+	 * L4D2-launcher patch: returns a 64 KB zero-filled MTLBuffer used as the
+	 * fallback target for descriptor slots that the application leaves unbound
+	 * but a shader still references.  Without this, MoltenVK writes nil/0 into
+	 * the argument buffer; the GPU then dereferences address 0 and the command
+	 * buffer faults with "Internal Error (0000010c)".  Pointing those slots at
+	 * a real (large, zero-filled) buffer makes the shader read zeros instead
+	 * of faulting — same semantics as VK_EXT_robustness2 nullDescriptor.
+	 */
+	id<MTLBuffer> getDummyNullDescriptorMTLBuffer();
+
+	/**
+	 * L4D2-launcher patch: same purpose as the dummy buffer, but for texture
+	 * slots.  A 1x1 RGBA8 zero-filled 2D texture with read+write usage
+	 * covers both sampled and storage descriptor types.
+	 */
+	id<MTLTexture> getDummyNullDescriptorMTLTexture();
+
+	/**
+	 * L4D2-launcher patch: dedicated sampler for null sampler descriptor
+	 * slots.  Uses linear filtering + repeat addressing so that any shader
+	 * sampling through this fallback gets filtered output (not pixelated /
+	 * nearest-neighbor like the bare default MTLSamplerState).  Important
+	 * for particle/sprite shaders where the visual quality of the null-slot
+	 * fallback shows through.
+	 */
+	id<MTLSamplerState> getDummyNullDescriptorMTLSampler();
+
 	/**
 	 * Returns whether MTLCommandBuffers can be prefilled.
 	 *
@@ -1096,6 +1124,9 @@ protected:
 	std::string _capturePipeFileName;
 	id<MTLSamplerState> _defaultMTLSamplerState = nil;
 	id<MTLBuffer> _dummyBlitMTLBuffer = nil;
+	id<MTLBuffer> _dummyNullDescriptorMTLBuffer = nil;	// L4D2-launcher patch
+	id<MTLTexture> _dummyNullDescriptorMTLTexture = nil;	// L4D2-launcher patch
+	id<MTLSamplerState> _dummyNullDescriptorMTLSampler = nil;	// L4D2-launcher patch
 #if MVK_XCODE_16
 	id<MTLResidencySet> _residencySet = nil;
 #endif
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
index e1f3c04..e1c987e 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
@@ -112,7 +112,19 @@ MVKMTLDeviceCapabilities::MVKMTLDeviceCapabilities(id<MTLDevice> mtlDev) {
 #endif
 	supportsMac2 = MVK_MACOS;	// Incl Mac2 & MacCatalyst2
 
-	isAppleGPU = supportsApple1;
+	// L4D2-launcher patch: modern Apple Silicon (M2 Pro / M3 / M4 etc.)
+	// no longer reports MTLGPUFamilyApple1 support in some Metal versions,
+	// so `supportsApple1` returns false on M4 Pro even though it IS an
+	// Apple GPU.  That broke our robustImageAccess2 advertisement (and a
+	// dozen other code paths that gate Apple-GPU-only behavior on this
+	// flag).  Use ANY Apple family as the indicator.
+	isAppleGPU = supportsApple1 || supportsApple2 || supportsApple3 ||
+				 supportsApple4 || supportsApple5 || supportsApple6 ||
+				 supportsApple7 || supportsApple8 || supportsApple9
+#if MVK_XCODE_26
+				 || supportsApple10
+#endif
+				 ;
 
 	if ([mtlDev respondsToSelector: @selector(supportsBCTextureCompression)]) {
 		supportsBCTextureCompression = mtlDev.supportsBCTextureCompression;
@@ -626,9 +638,19 @@ void MVKPhysicalDevice::getFeatures(VkPhysicalDeviceFeatures2* features) {
 			}
 			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_KHR: {
 				auto* robustness2Features = (VkPhysicalDeviceRobustness2FeaturesKHR*)next;
+				// L4D2-launcher patch:
+				// - robustBufferAccess2 stays FALSE: Apple Silicon Metal does
+				//   NOT do safe OOB buffer reads/writes.  Advertising true
+				//   caused DXVK to emit SPIR-V assuming the driver would
+				//   bounds-check buffers, leading to Metal command buffer
+				//   crashes with "Internal Error (0000010c)" on OOB reads.
+				// - robustImageAccess2 stays = isAppleGPU: Metal DOES safely
+				//   clamp out-of-bounds image accesses on Apple GPUs.
+				// - nullDescriptor stays = true (our existing patch — Metal
+				//   gets a real zero-filled buffer for unbound descriptors).
 				robustness2Features->robustBufferAccess2 = false;
 				robustness2Features->robustImageAccess2 = _gpuCapabilities.isAppleGPU;
-				robustness2Features->nullDescriptor = false;
+				robustness2Features->nullDescriptor = true;
 				break;
 			}
 			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FMA_FEATURES_KHR: {
@@ -2551,7 +2573,7 @@ void MVKPhysicalDevice::initMetalFeatures() {
 		_metalFeatures.maxPerStageTextureCount = 128;
 
 		_metalFeatures.astcHDRTextures = !MVK_TVOS || mvkOSVersionIsAtLeast(16.0);
-		_metalFeatures.residencySets = mvkOSVersionIsAtLeast(15.0, 18.0, 2.0);
+		_metalFeatures.residencySets = mvkOSVersionIsAtLeast(15.0, 18.0, 2.0) || (getenv("L4D2_MVK_FORCE_RESIDENCY") != nullptr); // L4D2: force the efficient residency-set path (real macOS 26 supports it; wine clamps the reported OS to 10.16 and hides it) WITHOUT any MSL/OS-version change, so rendering is unaffected. Candidate fix for the heavy-HDR-frame resource-pressure 0x010c (used only with Metal argument buffers; pair with L4D2_MVK_MAB=1).
 		_metalFeatures.simdPermute = true;
 	}
 
@@ -3369,7 +3391,35 @@ MVK_PUBLIC_SYMBOL MTLStorageMode MVKPhysicalDevice::getMTLStorageModeFromVkMemor
 	if ( !mvkAreAllFlagsEnabled(vkFlags, VK_MEMORY_PROPERTY_HOST_VISIBLE_BIT) ) {
 #if !MVK_OS_SIMULATOR
 		if (_gpuCapabilities.isAppleGPU && mvkAreAllFlagsEnabled(vkFlags, VK_MEMORY_PROPERTY_LAZILY_ALLOCATED_BIT)) {
-			return MTLStorageModeMemoryless;
+			// L4D2-launcher fix: MTLStorageModeMemoryless backs the resource in
+			// TILE memory, which is allocated at render-pass EXECUTION time and
+			// is a tiny pool entirely separate from device memory.  When a
+			// pass's combined memoryless attachment footprint exceeds the Apple
+			// GPU tile budget, the kernel fails the lazy allocation with
+			//   IOGPUResource::newResourceWithOptions: IOGPUSysMemory::withOptions failed
+			// and aborts the command buffer as "Internal Error (0000010c)" — even
+			// with 18 GB of device memory free, because tile memory is a separate
+			// pool.  L4D2's first full-scene gameplay frame trips this every time
+			// → VK_ERROR_DEVICE_LOST → the game renders one frame then freezes.
+			// Force real Private (device-memory) backing: the transient
+			// attachment is semantically identical to the app, just stored in
+			// regular VRAM, so there is no execution-time tile allocation that
+			// can fail.  Costs a little bandwidth (the attachment is written out
+			// rather than kept on-tile) — negligible for a 2009-era game with
+			// gigabytes free.  Set MVK_L4D2_FORCE_PRIVATE_RT=0 to A/B back to the
+			// stock memoryless behavior without rebuilding.
+			// DEFAULT REVERTED to stock memoryless: forcing Private made the
+			// allocation fault hit EARLIER (during load vs. first frame),
+			// proving the freeze is GPU memory/resource PRESSURE, not the
+			// memoryless tile budget — Private just adds more resident real
+			// memory and trips the limit sooner.  Keep memoryless (lighter,
+			// gets further).  Set MVK_L4D2_FORCE_PRIVATE_RT=1 to force Private.
+			static int s_force_private = -1;
+			if (s_force_private < 0) {
+				const char* ev = getenv("MVK_L4D2_FORCE_PRIVATE_RT");
+				s_force_private = (ev && ev[0] == '1') ? 1 : 0;
+			}
+			return s_force_private ? MTLStorageModePrivate : MTLStorageModeMemoryless;
 		}
 #endif
 		return MTLStorageModePrivate;
@@ -4854,6 +4904,82 @@ id<MTLBuffer> MVKDevice::getDummyBlitMTLBuffer() {
 	return _dummyBlitMTLBuffer;
 }
 
+// L4D2-launcher patch: see header for rationale.  64 KB matches Vulkan's
+// MaxUniformBufferSize so any UBO-style shader read into this fallback stays
+// in-bounds; storage mode Shared lets us memset zeros from the CPU.
+id<MTLBuffer> MVKDevice::getDummyNullDescriptorMTLBuffer() {
+	if ( !_dummyNullDescriptorMTLBuffer ) {
+		lock_guard<mutex> lock(_rezLock);
+		if ( !_dummyNullDescriptorMTLBuffer ) {
+			@autoreleasepool {
+				const NSUInteger kSize = 65536;
+				_dummyNullDescriptorMTLBuffer = [_physicalDevice->_mtlDevice newBufferWithLength: kSize options: MTLResourceStorageModeShared];
+				if (_dummyNullDescriptorMTLBuffer.contents) {
+					memset(_dummyNullDescriptorMTLBuffer.contents, 0, kSize);
+				}
+				_dummyNullDescriptorMTLBuffer.label = @"MVKDummyNullDescriptorBuffer";
+			}
+		}
+	}
+	return _dummyNullDescriptorMTLBuffer;
+}
+
+// L4D2-launcher patch: 1x1 RGBA8 zero-filled 2D texture; covers the common
+// shader sample path where DXVK leaves a texture descriptor null because the
+// D3D9 source shader didn't bind that sampler slot.  ShaderRead+Write so
+// storage-image null slots work — stripping Write made particles/muzzle
+// flashes silently disappear because their compute shaders rely on writing
+// to a render target that DXVK was binding null.  The per-submit blit-zero
+// in MVKQueue still cleans up between submits.
+id<MTLTexture> MVKDevice::getDummyNullDescriptorMTLTexture() {
+	if ( !_dummyNullDescriptorMTLTexture ) {
+		lock_guard<mutex> lock(_rezLock);
+		if ( !_dummyNullDescriptorMTLTexture ) {
+			@autoreleasepool {
+				MTLTextureDescriptor* desc = [MTLTextureDescriptor texture2DDescriptorWithPixelFormat: MTLPixelFormatRGBA8Unorm
+																								 width: 1
+																								height: 1
+																							 mipmapped: NO];
+				desc.usage = MTLTextureUsageShaderRead | MTLTextureUsageShaderWrite;
+				desc.storageMode = MTLStorageModeShared;
+				_dummyNullDescriptorMTLTexture = [_physicalDevice->_mtlDevice newTextureWithDescriptor: desc];
+				uint32_t zeros = 0;
+				[_dummyNullDescriptorMTLTexture replaceRegion: MTLRegionMake2D(0, 0, 1, 1)
+												  mipmapLevel: 0
+													withBytes: &zeros
+												  bytesPerRow: 4];
+				_dummyNullDescriptorMTLTexture.label = @"MVKDummyNullDescriptorTexture";
+			}
+		}
+	}
+	return _dummyNullDescriptorMTLTexture;
+}
+
+// L4D2-launcher patch: dedicated null sampler with linear filtering and
+// repeat addressing.  Source-engine particle/sprite shaders that sample
+// through a null slot end up using this; linear filtering matches what
+// most rendering paths expect, avoiding the blocky look you'd get from
+// the bare default MTLSamplerState (nearest-neighbor).
+id<MTLSamplerState> MVKDevice::getDummyNullDescriptorMTLSampler() {
+	if ( !_dummyNullDescriptorMTLSampler ) {
+		lock_guard<mutex> lock(_rezLock);
+		if ( !_dummyNullDescriptorMTLSampler ) {
+			@autoreleasepool {
+				MTLSamplerDescriptor* desc = [[MTLSamplerDescriptor new] autorelease];
+				desc.minFilter = MTLSamplerMinMagFilterLinear;
+				desc.magFilter = MTLSamplerMinMagFilterLinear;
+				desc.mipFilter = MTLSamplerMipFilterLinear;
+				desc.sAddressMode = MTLSamplerAddressModeRepeat;
+				desc.tAddressMode = MTLSamplerAddressModeRepeat;
+				desc.rAddressMode = MTLSamplerAddressModeRepeat;
+				desc.supportArgumentBuffers = _physicalDevice->_isUsingMetalArgumentBuffers;
+				_dummyNullDescriptorMTLSampler = [_physicalDevice->_mtlDevice newSamplerStateWithDescriptor: desc];
+			}
+		}
+	}
+	return _dummyNullDescriptorMTLSampler;
+}
+
 MTLCompileOptions* MVKDevice::getMTLCompileOptions(uint32_t fpFastMathFlags,
 												   bool preserveInvariance) {
 	MTLCompileOptions* mtlCompOpt = [MTLCompileOptions new];
@@ -4917,6 +5043,21 @@ void MVKDevice::startAutoGPUCapture(MVKConfigAutoGPUCaptureScope autoGPUCaptureS
 
 	if (_isCurrentlyAutoGPUCapturing || (getMVKConfig().autoGPUCaptureScope != autoGPUCaptureScope)) { return; }
 
+	// L4D2-launcher: defer DEVICE-scope capture start by MVK_L4D2_CAPTURE_DELAY_SEC seconds so the
+	// trace covers only the post-load frames around the 0x010c fault (small + Xcode-openable) instead
+	// of the whole ~40s session (11 GB of menu/loading). Re-triggered per-present from MVKQueue;
+	// the device-loss stop still finalizes the trace exactly at the fault.
+	if (autoGPUCaptureScope == MVK_CONFIG_AUTO_GPU_CAPTURE_SCOPE_DEVICE) {
+		static double s_l4d2CapDelaySec = -1.0;
+		if (s_l4d2CapDelaySec < 0.0) { const char* dEnv = getenv("MVK_L4D2_CAPTURE_DELAY_SEC"); s_l4d2CapDelaySec = dEnv ? atof(dEnv) : 0.0; }
+		if (s_l4d2CapDelaySec > 0.0) {
+			static uint64_t s_l4d2CapFirstNanos = 0;
+			uint64_t nowNanos = mvkGetContinuousNanoseconds();
+			if (s_l4d2CapFirstNanos == 0) { s_l4d2CapFirstNanos = nowNanos; return; }
+			if ((double)(nowNanos - s_l4d2CapFirstNanos) < s_l4d2CapDelaySec * 1.0e9) { return; }
+		}
+	}
+
 	if (autoGPUCaptureScope == MVK_CONFIG_AUTO_GPU_CAPTURE_SCOPE_ON_DEMAND && !readGPUCapturePipe()) return;
 
 	_isCurrentlyAutoGPUCapturing = true;
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDeviceMemory.mm b/MoltenVK/MoltenVK/GPUObjects/MVKDeviceMemory.mm
index 27d634f..a248952 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDeviceMemory.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDeviceMemory.mm
@@ -189,6 +189,23 @@ void MVKDeviceMemory::removeImageMemoryBinding(MVKDeviceMemory** pMem, MVKImageM
 
 // Ensures that this instance is backed by a MTLHeap object,
 // creating the MTLHeap if needed, and returns whether it was successful.
+// L4D2-launcher diagnostic: running tally of MTL-backed VkDeviceMemory so the
+// command-buffer fault handler (MVKQueue.mm) can report how much GPU memory was
+// allocated at the moment of an IOGPUSysMemory::withOptions / 0x010c abort —
+// which distinguishes a true out-of-memory (tally near device size) from a
+// limit/internal failure (tally small).  Plain globals: a few races only skew
+// the ballpark, which is all we need.  External linkage so MVKQueue can read.
+uint64_t g_mvkL4D2AllocBytes = 0;
+uint32_t g_mvkL4D2AllocCount = 0;
+static int s_l4d2_alloc_dbg = -1;
+static bool l4d2AllocDbg() {
+	if (s_l4d2_alloc_dbg < 0) {
+		const char* ev = getenv("MVK_L4D2_DEBUG");
+		s_l4d2_alloc_dbg = (ev && ev[0] == '1') ? 1 : 0;
+	}
+	return s_l4d2_alloc_dbg != 0;
+}
+
 bool MVKDeviceMemory::ensureMTLHeap() {
 
 	if (_mtlHeap) { return true; }
@@ -224,7 +241,18 @@ bool MVKDeviceMemory::ensureMTLHeap() {
 	heapDesc.size = _allocationSize;
 	_mtlHeap = [getMTLDevice() newHeapWithDescriptor: heapDesc];	// retained
 	[heapDesc release];
-	if (!_mtlHeap) { return false; }
+	if (!_mtlHeap) {
+		fprintf(stderr, "[mvk-l4d2-alloc] HEAP alloc FAILED: size=%.1f MB mode=%lu "
+						"(running total before fail: %.1f MB across %u allocs)\n",
+				_allocationSize/1048576.0, (unsigned long)_mtlStorageMode,
+				g_mvkL4D2AllocBytes/1048576.0, g_mvkL4D2AllocCount);
+		return false;
+	}
+	g_mvkL4D2AllocBytes += _allocationSize; g_mvkL4D2AllocCount++;
+	if (l4d2AllocDbg())
+		fprintf(stderr, "[mvk-l4d2-alloc] HEAP %.1f MB mode=%lu  → total=%.1f MB / %u allocs\n",
+				_allocationSize/1048576.0, (unsigned long)_mtlStorageMode,
+				g_mvkL4D2AllocBytes/1048576.0, g_mvkL4D2AllocCount);
 
 	propagateDebugName();
 
@@ -239,7 +267,11 @@ bool MVKDeviceMemory::ensureMTLBuffer() {
 
 	NSUInteger memLen = mvkAlignByteCount(_allocationSize, getMetalFeatures().mtlBufferAlignment);
 
-	if (memLen > getMetalFeatures().maxMTLBufferSize) { return false; }
+	if (memLen > getMetalFeatures().maxMTLBufferSize) {
+		fprintf(stderr, "[mvk-l4d2-alloc] BUFFER request %.1f MB EXCEEDS maxMTLBufferSize %.1f MB — rejected\n",
+				memLen/1048576.0, getMetalFeatures().maxMTLBufferSize/1048576.0);
+		return false;
+	}
 
 	id<MTLBuffer> buf;
 	// If host memory was already allocated, it is copied into the new MTLBuffer, and then released.
@@ -261,7 +293,19 @@ bool MVKDeviceMemory::ensureMTLBuffer() {
 	} else {
 		buf = [getMTLDevice() newBufferWithLength: memLen options: getMTLResourceOptions()];     // retained
 	}
-	if (!buf) { return false; }
+	if (!buf) {
+		fprintf(stderr, "[mvk-l4d2-alloc] BUFFER alloc FAILED: size=%.1f MB heap=%p "
+						"(running total before fail: %.1f MB across %u allocs)\n",
+				memLen/1048576.0, _mtlHeap,
+				g_mvkL4D2AllocBytes/1048576.0, g_mvkL4D2AllocCount);
+		return false;
+	}
+	g_mvkL4D2AllocBytes += memLen; g_mvkL4D2AllocCount++;
+	if (l4d2AllocDbg())
+		fprintf(stderr, "[mvk-l4d2-alloc] BUFFER %.1f MB mode=%lu%s  → total=%.1f MB / %u allocs\n",
+				memLen/1048576.0, (unsigned long)_mtlStorageMode,
+				_mtlHeap ? " (in-heap)" : "",
+				g_mvkL4D2AllocBytes/1048576.0, g_mvkL4D2AllocCount);
 	_device->makeResident(buf);
 	_device->getLiveResources().add(buf);
 	_pMemory = isMemoryHostAccessible() ? buf.contents : nullptr;
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKPipeline.mm b/MoltenVK/MoltenVK/GPUObjects/MVKPipeline.mm
index 7357ff1..c64ed05 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKPipeline.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKPipeline.mm
@@ -377,8 +377,19 @@ MVKPipelineLayout* MVKPipelineLayout::Create(MVKDevice* device, const VkPipeline
 
 	for (uint32_t stage = 0; stage < kMVKShaderStageCount; stage++) {
 		ret->_pushConstantResourceIndices[stage] = static_cast<uint8_t>(ret->_mtlResourceCounts.stages[stage].bufferIndex);
-		if (ret->stageUsesPushConstants(static_cast<MVKShaderStage>(stage)))
-			++ret->_mtlResourceCounts.stages[stage].bufferIndex;
+		// L4D2-launcher patch: ALWAYS reserve one Metal buffer slot per stage
+		// for the push-constant range, even if this stage's shader variant
+		// doesn't reference push constants.  Required for DXVK 1.10.3 D3D9
+		// path: DXVK shares a pipeline layout across multiple shader variants
+		// where SOME use the render_state push constants and some don't.
+		// Without this, the MSL converter assigns descriptor-set-0's first
+		// uniform buffer (cbuffer_t) to Metal slot 0, colliding with the
+		// push-constant struct also at slot 0, and Metal rejects with
+		// "cannot reserve 'buffer' resource location at index 0".  Cost: one
+		// wasted Metal buffer slot per stage (out of 31 available) when the
+		// shader doesn't use push constants — free.  Fixes wide swath of
+		// fragment-shader pipeline failures on Apple Silicon under MAB=off.
+		++ret->_mtlResourceCounts.stages[stage].bufferIndex;
 	}
 
 	for (size_t i = 0; i < layouts.size(); i++) {
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKQueue.h b/MoltenVK/MoltenVK/GPUObjects/MVKQueue.h
index a69ebb0..8d66a7d 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKQueue.h
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKQueue.h
@@ -247,7 +247,11 @@ class MVKQueueCommandBufferSubmission : public MVKQueueSubmission {
 public:
 	VkResult execute() override;
 
-	MVKQueueCommandBufferSubmission(MVKQueue* queue, 
+	// L4D2-launcher: commit the active MTLCommandBuffer (no completion signal) and start a fresh one,
+	// returning it — used to split an oversized command buffer mid-encode.
+	id<MTLCommandBuffer> l4d2RotateActiveMTLCommandBuffer();
+
+	MVKQueueCommandBufferSubmission(MVKQueue* queue,
 									const VkSubmitInfo2* pSubmit,
 									VkFence fence, 
 									MVKCommandUse cmdUse);
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKQueue.mm b/MoltenVK/MoltenVK/GPUObjects/MVKQueue.mm
index 42e5646..21c912a 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKQueue.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKQueue.mm
@@ -169,7 +169,16 @@ id<MTLCommandBuffer> MVKQueue::getMTLCommandBuffer(MVKCommandUse cmdUse, bool re
 
 	MTLCommandBufferDescriptor* mtlCmdBuffDesc = [MTLCommandBufferDescriptor new];	// temp retain
 	mtlCmdBuffDesc.retainedReferences = retainRefs;
-	if (getMVKConfig().debugMode) {
+	// L4D2-launcher patch: record per-encoder error info so we can diagnose
+	// MTLCommandBufferErrorInternal (0x010c) failures by encoder name +
+	// signposts.  Gated behind MVK_L4D2_DEBUG=1 (checked once) OR MoltenVK's
+	// own debugMode, so the default play path pays nothing.
+	static int s_mvk_l4d2_debug = -1;
+	if (s_mvk_l4d2_debug < 0) {
+		const char* ev = getenv("MVK_L4D2_DEBUG");
+		s_mvk_l4d2_debug = (ev && ev[0] == '1') ? 1 : 0;
+	}
+	if (getMVKConfig().debugMode || s_mvk_l4d2_debug) {
 		mtlCmdBuffDesc.errorOptions |= MTLCommandBufferErrorOptionEncoderExecutionStatus;
 	}
 	mtlCmdBuff = [_mtlQueue commandBufferWithDescriptor: mtlCmdBuffDesc];
@@ -178,7 +187,15 @@ id<MTLCommandBuffer> MVKQueue::getMTLCommandBuffer(MVKCommandUse cmdUse, bool re
 	addPerformanceInterval(getPerformanceStats().queue.retrieveMTLCommandBuffer, startTime);
 	NSString* mtlCmdBuffLabel = getMTLCommandBufferLabel(cmdUse);
 	setMetalObjectLabel(mtlCmdBuff, mtlCmdBuffLabel);
-	[mtlCmdBuff addCompletedHandler: ^(id<MTLCommandBuffer> mtlCB) { handleMTLCommandBufferError(mtlCB); }];
+	[mtlCmdBuff addCompletedHandler: ^(id<MTLCommandBuffer> mtlCB) {
+		static int s_tm = -1;
+		if (s_tm < 0) { const char* tm_e = getenv("MVK_L4D2_DEBUG"); s_tm = (tm_e && tm_e[0] == '1') ? 1 : 0; }
+		if (s_tm) {
+			double tm_ms = (mtlCB.GPUEndTime - mtlCB.GPUStartTime) * 1000.0;
+			if (tm_ms > 100.0) { fprintf(stderr, "[mvk-l4d2-time] cmdbuf GPU exec %.1f ms (status %ld)\n", tm_ms, (long)mtlCB.status); }
+		}
+		handleMTLCommandBufferError(mtlCB);
+	}];
 
 	if ( !mtlCmdBuff ) { reportError(VK_ERROR_OUT_OF_POOL_MEMORY, "%s could not be acquired.", mtlCmdBuffLabel.UTF8String); }
 	return mtlCmdBuff;
@@ -257,26 +274,83 @@ void MVKQueue::handleMTLCommandBufferError(id<MTLCommandBuffer> mtlCmdBuff) {
 				 (mtlCmdBuff.label ? mtlCmdBuff.label.UTF8String : ""),
 				 mtlCmdBuff.error.code, mtlCmdBuff.error.localizedDescription.UTF8String);
 
+	// L4D2-launcher patch: log per-encoder error info + shader logs to stderr,
+	// gated behind the MVK_L4D2_DEBUG=1 environment variable.  This is THE
+	// diagnostic for Apple Silicon command-buffer failures (it identifies the
+	// exact Metal encoder / Source render pass that faulted), but it's noisy
+	// when a fault repeats every frame, so it's off by default.  Checked once
+	// and cached.  Paired with the unconditional errorOptions |= Encoder
+	// ExecutionStatus in getMTLCommandBuffer() so the info is captured even
+	// outside MoltenVK's own debugMode.
+	static int s_mvk_l4d2_debug = -1;
+	if (s_mvk_l4d2_debug < 0) {
+		const char* ev = getenv("MVK_L4D2_DEBUG");
+		s_mvk_l4d2_debug = (ev && ev[0] == '1') ? 1 : 0;
+	}
+	if (!s_mvk_l4d2_debug) { return; }
+
+	static int s_logged_encoder_info = 0;
 	if (NSArray<id<MTLCommandBufferEncoderInfo>>* mtlEncInfo = mtlCmdBuff.error.userInfo[MTLCommandBufferEncoderInfoErrorKey]) {
-		MVKLogInfo("Encoders for %p \"%s\":", mtlCmdBuff, mtlCmdBuff.label ? mtlCmdBuff.label.UTF8String : "");
+		fprintf(stderr, "[mvk-l4d2-patch] Encoders for %p \"%s\" (err code %ld: %s):\n", mtlCmdBuff,
+				mtlCmdBuff.label ? mtlCmdBuff.label.UTF8String : "",
+				(long)mtlCmdBuff.error.code,
+				mtlCmdBuff.error.localizedDescription.UTF8String);
 		for (id<MTLCommandBufferEncoderInfo> enc in mtlEncInfo) {
-			MVKLogInfo(" - %s: %s", enc.label.UTF8String, mvkStringFromMTLCommandEncoderErrorState(enc.errorState));
+			fprintf(stderr, "[mvk-l4d2-patch]  - %s: %s\n",
+					enc.label.UTF8String,
+					mvkStringFromMTLCommandEncoderErrorState(enc.errorState));
 			if (enc.debugSignposts.count > 0) {
-				MVKLogInfo("   Debug signposts:");
+				fprintf(stderr, "[mvk-l4d2-patch]    Debug signposts:\n");
 				for (NSString* signpost in enc.debugSignposts) {
-					MVKLogInfo("    - %s", signpost.UTF8String);
+					fprintf(stderr, "[mvk-l4d2-patch]     - %s\n", signpost.UTF8String);
+				}
+			}
+		}
+		s_logged_encoder_info++;
+	} else if (s_logged_encoder_info < 3) {
+		// No per-encoder info (Metal discards it on a hard GPU memory fault).
+		// Dump the FULL userInfo dictionary instead — for an allocation/OOM
+		// abort (IOGPUSysMemory::withOptions failed) the real signal is in
+		// NSUnderlyingError, whose domain+code identify the failure class
+		// (e.g. an IOAccelerator/IOGPU error vs. a true OOM).  This is what
+		// finally names a 0x010c that has no encoder attribution.
+		NSError* err = mtlCmdBuff.error;
+		fprintf(stderr, "[mvk-l4d2-patch] cmdbuf %p error has no encoder info "
+						"(domain %s, code %ld: %s, %lu userInfo keys):\n",
+				mtlCmdBuff, err.domain.UTF8String, (long)err.code,
+				err.localizedDescription.UTF8String,
+				(unsigned long)err.userInfo.count);
+		for (NSString* key in err.userInfo) {
+			id val = err.userInfo[key];
+			fprintf(stderr, "[mvk-l4d2-patch]    userInfo[%s] = %s\n",
+					key.UTF8String,
+					[[val description] UTF8String]);
+			// Recurse one level into the underlying error — that's where the
+			// IOAccelerator/IOGPU sub-code lives.
+			if ([val isKindOfClass:[NSError class]]) {
+				NSError* ue = (NSError*)val;
+				fprintf(stderr, "[mvk-l4d2-patch]      underlying: domain %s code %ld (%s)\n",
+						ue.domain.UTF8String, (long)ue.code,
+						ue.localizedDescription.UTF8String);
+				for (NSString* uk in ue.userInfo) {
+					fprintf(stderr, "[mvk-l4d2-patch]        underlying.userInfo[%s] = %s\n",
+							uk.UTF8String, [[ue.userInfo[uk] description] UTF8String]);
 				}
 			}
 		}
+		// L4D2-launcher diagnostic: how much GPU memory was allocated when the
+		// fault hit?  Near device size → true OOM; small → a limit/internal
+		// failure (resource count, address space, residency).  Tally lives in
+		// MVKDeviceMemory.mm.
+		extern uint64_t g_mvkL4D2AllocBytes;
+		extern uint32_t g_mvkL4D2AllocCount;
+		fprintf(stderr, "[mvk-l4d2-patch]    GPU mem allocated at fault: %.1f MB across %u VkDeviceMemory allocs\n",
+				g_mvkL4D2AllocBytes/1048576.0, g_mvkL4D2AllocCount);
+		s_logged_encoder_info++;
 	}
 
-	bool isFirstMsg = true;
 	for (id<MTLFunctionLog> log in mtlCmdBuff.logs) {
-		if (isFirstMsg) {
-			MVKLogInfo("Shader log messages:");
-			isFirstMsg = false;
-		}
-		MVKLogInfo("%s", log.description.UTF8String);
+		fprintf(stderr, "[mvk-l4d2-patch] shader log: %s\n", log.description.UTF8String);
 	}
 }
 
@@ -465,6 +539,16 @@ VkResult MVKQueueCommandBufferSubmission::execute() {
 
 	_queue->_submissionCaptureScope->beginScope();
 
+	// L4D2-launcher patch: REVERTED 2026-05-24 — per-submit blit-zero of the
+	// null dummies caused regression to constant black flashes.  Most likely
+	// cause: the wrapper CB carrying the blit gets committed AFTER the game's
+	// CBs in the same submit, so the zero takes effect only for the NEXT
+	// submit's game work — meaning the active submit still uses whatever was
+	// in the dummies from before.  Combined with R+W texture usage Metal
+	// hazard tracking flagged the cross-encoder access patterns and the
+	// command buffers were failing under load.  Reverting; cross-shader
+	// dummy pollution → occasional rainbow corruption is the accepted trade.
+
 	// If using encoded semaphore waiting, do so now.
 	for (auto& ws : _waitSemaphores) { ws.encodeWait(getActiveMTLCommandBuffer()); }
 
@@ -554,6 +638,21 @@ VkResult MVKQueueCommandBufferSubmission::commitActiveMTLCommandBuffer(bool sign
 	// Retrieve the result before committing MTLCommandBuffer, because finish() will destroy this instance.
 	VkResult rslt = mtlCmdBuff ? getConfigurationResult() : VK_ERROR_OUT_OF_POOL_MEMORY;
 	[mtlCmdBuff commit];
+	// L4D2-launcher: under MVK_L4D2_DEBUG, submit synchronously so the 0x010c fault fires in lockstep
+	// with encoding. The [mvk-tiledbg] render-pass lines logged immediately before this buffer's
+	// "FAULTED" line are the faulting buffer's passes — attribution without Xcode. Slow (no CPU/GPU
+	// overlap), diagnostic-only.
+	{
+		static int s_l4d2Sync = -1;
+		if (s_l4d2Sync < 0) { const char* sEnv = getenv("MVK_L4D2_SYNC"); s_l4d2Sync = (sEnv && sEnv[0] == '1') ? 1 : 0; }
+		if (s_l4d2Sync && mtlCmdBuff) {
+			static unsigned long long s_l4d2CbIdx = 0;
+			unsigned long long idx = ++s_l4d2CbIdx;
+			[mtlCmdBuff waitUntilCompleted];
+			if ((long)mtlCmdBuff.status == 5) { fprintf(stderr, "[mvk-l4d2-sync] render cmdbuf #%llu FAULTED (status=Error)\n", idx); }
+			else { fprintf(stderr, "[mvk-l4d2-sync] render cmdbuf #%llu ok (status=%ld)\n", idx, (long)mtlCmdBuff.status); }
+		}
+	}
 	[mtlCmdBuff release];		// retained
 
 	// If we need to signal completion, but an error occurred and the MTLCommandBuffer
@@ -563,6 +662,15 @@ VkResult MVKQueueCommandBufferSubmission::commitActiveMTLCommandBuffer(bool sign
 	return rslt;
 }
 
+// L4D2-launcher: commit the current active MTLCommandBuffer (without signalling completion, so the
+// final commit still signals the fence/semaphores) and lazily start a fresh active one. Queue command
+// buffers execute serially in commit order, so splitting an oversized submission across several Metal
+// command buffers preserves execution ordering without extra synchronization.
+id<MTLCommandBuffer> MVKQueueCommandBufferSubmission::l4d2RotateActiveMTLCommandBuffer() {
+	if (_activeMTLCommandBuffer) { commitActiveMTLCommandBuffer(false); }
+	return getActiveMTLCommandBuffer();
+}
+
 // Be sure to retain() any API objects referenced in this function, and release() them in the
 // destructor (or superclass destructor). It is possible for rare race conditions to result
 // in the app destroying API objects before this function completes execution. For example,
@@ -734,6 +842,9 @@ VkResult MVKQueuePresentSurfaceSubmission::execute() {
 		_queue->_index == getMVKConfig().defaultGPUCaptureScopeQueueIndex) {
 		getDevice()->stopAutoGPUCapture(MVK_CONFIG_AUTO_GPU_CAPTURE_SCOPE_ON_DEMAND);
 		getDevice()->startAutoGPUCapture(MVK_CONFIG_AUTO_GPU_CAPTURE_SCOPE_ON_DEMAND, _queue->getMTLCommandQueue());
+		// L4D2-launcher: re-trigger deferred DEVICE-scope capture each present (the delay gate lives in
+		// startAutoGPUCapture) so a DEVICE-scope capture can begin AFTER load, near the 0x010c fault.
+		getDevice()->startAutoGPUCapture(MVK_CONFIG_AUTO_GPU_CAPTURE_SCOPE_DEVICE, _queue->getMTLCommandQueue());
 	}
 
 	if ( !mtlCmdBuff ) { setConfigurationResult(VK_ERROR_OUT_OF_POOL_MEMORY); }	// Check after images may set error.
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKRenderPass.mm b/MoltenVK/MoltenVK/GPUObjects/MVKRenderPass.mm
index 8c4497f..c14ecf4 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKRenderPass.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKRenderPass.mm
@@ -277,8 +277,63 @@ void MVKRenderSubpass::populateMTLRenderPassDescriptor(MTLRenderPassDescriptor*
 		}
 	}
 
+	// L4D2-launcher: per-render-pass tile-memory footprint (MVK_L4D2_DEBUG=1 / --diag-gfx).
+	// The 0x010c is a memoryless tile-budget overflow at the first heavy HDR frame; log each
+	// pass's combined attachment bytes-per-pixel x samples to find the over-budget pass.
+	{
+		static int s_td = -1;
+		if (s_td < 0) { const char* td_e = getenv("MVK_L4D2_DEBUG"); s_td = (td_e && td_e[0] == '1') ? 1 : 0; }
+		if (s_td) {
+			uint32_t td_samples = mvkSampleCountFromVkSampleCountFlagBits(getSampleCount());
+			if ( !td_samples ) { td_samples = 1; }
+			size_t td_perPixel = 0; int td_nAtt = 0; unsigned long td_firstFmt = 0;
+			for (uint32_t td_ci = 0; td_ci < caCnt; td_ci++) {
+				uint32_t td_ai = _colorAttachments[td_ci].attachment;
+				if (td_ai != VK_ATTACHMENT_UNUSED) {
+					MTLPixelFormat td_f = attachments[td_ai]->getMTLPixelFormat();
+					if ( !td_nAtt ) { td_firstFmt = (unsigned long)td_f; }
+					td_perPixel += pixFmts->getBytesPerBlock(td_f);
+					td_nAtt++;
+				}
+			}
+			if (depthRPAttIdx != VK_ATTACHMENT_UNUSED) {
+				td_perPixel += pixFmts->getBytesPerBlock(attachments[depthRPAttIdx]->getMTLPixelFormat());
+			}
+			fprintf(stderr, "[mvk-tiledbg] pass=%u color=%d depth=%d samples=%u perPixel=%zub tile=%zub firstColorFmt=%lu\n",
+					passIdx, td_nAtt, (depthRPAttIdx != VK_ATTACHMENT_UNUSED) ? 1 : 0, td_samples,
+					td_perPixel, td_perPixel * td_samples, td_firstFmt);
+		}
+	}
+
 	if (caUsedCnt == 0 && depthRPAttIdx == VK_ATTACHMENT_UNUSED && stencilRPAttIdx == VK_ATTACHMENT_UNUSED) {
-        mtlRPDesc.defaultRasterSampleCount = mvkSampleCountFromVkSampleCountFlagBits(_defaultSampleCount);
+        uint32_t drsc = mvkSampleCountFromVkSampleCountFlagBits(_defaultSampleCount);
+        mtlRPDesc.defaultRasterSampleCount = drsc;
+        // L4D2-launcher fix: a Metal render pass with NO attachments REQUIRES renderTargetWidth and
+        // renderTargetHeight to be set (Apple spec) — MoltenVK set only defaultRasterSampleCount,
+        // leaving them 0, i.e. an invalid 0x0 render target. The Apple GPU aborts that command buffer
+        // as Internal Error 0x010c — the heavy-HDR-frame freeze (this attachment-less pass is encoded
+        // into the faulting command buffer). Derive the target size from the framebuffer.
+        VkExtent2D fbExtent = {0, 0};
+        if (framebuffer) {
+            fbExtent = framebuffer->getExtent2D();
+            if (fbExtent.width)  { mtlRPDesc.renderTargetWidth  = fbExtent.width; }
+            if (fbExtent.height) { mtlRPDesc.renderTargetHeight = fbExtent.height; }
+        }
+        // L4D2 fix candidate: DXVK creates attachment-less framebuffers at the Metal max
+        // (16384x16384). A render pass with a 16384x16384 logical area appears to trip the AGX
+        // 0x010c. Clamp the logical render-target size to a sane bound (>= screen). With no
+        // attachments this only limits the rasterizable area, which the occlusion-query draw
+        // (screen-space geometry) stays within, so the query result is unaffected.
+        if (mtlRPDesc.renderTargetWidth  == 0 || mtlRPDesc.renderTargetWidth  > 2048) { mtlRPDesc.renderTargetWidth  = 2048; }
+        if (mtlRPDesc.renderTargetHeight == 0 || mtlRPDesc.renderTargetHeight > 2048) { mtlRPDesc.renderTargetHeight = 2048; }
+        static int s_ald = -1;
+        if (s_ald < 0) { const char* e = getenv("MVK_L4D2_DEBUG"); s_ald = (e && e[0] == '1') ? 1 : 0; }
+        if (s_ald) {
+            fprintf(stderr, "[mvk-l4d2-noatt] attachment-less pass fb=%p fbExtent=%ux%u -> rtW=%lu rtH=%lu defaultRasterSampleCount=%u (vkDefaultSamples=%d)\n",
+                    (void*)framebuffer, fbExtent.width, fbExtent.height,
+                    (unsigned long)mtlRPDesc.renderTargetWidth, (unsigned long)mtlRPDesc.renderTargetHeight,
+                    drsc, (int)_defaultSampleCount);
+        }
 	}
 
 #if MVK_USE_METAL_PRIVATE_API
@@ -896,6 +951,13 @@ MTLStoreAction MVKAttachmentDescription::getMTLStoreAction(MVKRenderSubpass* sub
 														   bool isStencil,
 														   bool storeOverride) {
 
+	// L4D2-launcher diagnostic: optionally disable MSAA resolve store actions to test whether the
+	// aggregate of many MSAA resolves in the first full-scene HDR command buffer triggers the 0x010c.
+	// Gated by L4D2_MVK_NO_RESOLVE=1. (Breaks the resolved image; fault-test only.)
+	static int s_l4d2NoResolve = -1;
+	if (s_l4d2NoResolve < 0) { const char* e = getenv("L4D2_MVK_NO_RESOLVE"); s_l4d2NoResolve = (e && e[0] == '1') ? 1 : 0; }
+	if (s_l4d2NoResolve) { return isMemorylessAttachment ? MTLStoreActionDontCare : MTLStoreActionStore; }
+
 	// If the renderpass is going to be suspended, and resumed later, store the contents to preserve them until then.
 	if (mvkIsAnyFlagEnabled(_renderPass->getRenderingFlags(), VK_RENDERING_SUSPENDING_BIT)) {
 		return MTLStoreActionStore;
diff --git a/MoltenVK/MoltenVK/Utility/MVKFoundation.cpp b/MoltenVK/MoltenVK/Utility/MVKFoundation.cpp
index 6d718ba..c2ba548 100644
--- a/MoltenVK/MoltenVK/Utility/MVKFoundation.cpp
+++ b/MoltenVK/MoltenVK/Utility/MVKFoundation.cpp
@@ -135,7 +135,18 @@ const char* mvkVkComponentSwizzleName(VkComponentSwizzle swizzle) {
 	}
 }
 
-bool mvkSupportsBufferDeviceAddress() { return mvkOSVersionIsAtLeast(13.0, 16.0, 1.0); }
+bool mvkSupportsBufferDeviceAddress() {
+	// L4D2-launcher fix: this gates bufferDeviceAddress on macOS>=13, but the
+	// version comes from NSProcessInfo, which macOS CLAMPS to 10.16 inside the
+	// wine process (wine64 has a pre-11 deployment target). So on macOS 26 it
+	// wrongly returns false, disabling the Vulkan-1.2 feature DXVK 2.x requires
+	// -> "Failed to create D3D device". Apple Silicon on macOS 26 fully supports
+	// bufferDeviceAddress (Metal 3 GPU addresses), so force it on. Done here, not
+	// via SYSTEM_VERSION_COMPAT=0, because that env var flips the process OS
+	// context and triggers a Rosetta AOT re-translation of this dylib that fails
+	// code-signature attachment.
+	return true;
+}
 
 
 #pragma mark -
